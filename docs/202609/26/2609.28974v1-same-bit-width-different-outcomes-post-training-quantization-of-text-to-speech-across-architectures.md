---
title: "Same Bit Width, Different Outcomes: Post-Training Quantization of Text-to-Speech Across Architectures"
title_zh: 相同位宽，不同结果：跨架构文本到语音的训练后量化
authors: "Se Un Park, Yutae Kim, Junyoung Park"
date: 2026-09-24
pdf: "https://arxiv.org/pdf/2609.28974v1"
tags: ["query:el"]
score: 6.0
evidence: 跨架构训练后量化，并用逐层GPTQ恢复敏感组件
tldr: 后训练量化能降低端侧TTS成本，但已有评测通常只覆盖单一系统或方法。本文在统一协议下评测多种TTS架构，包括三个核心模型、八个权重与激活消融模型，以及两个盲测留出模型。结果显示，4比特逐通道权重使Supertonic的UTMOS下降2.8，而Kokoro仅下降0.07；逐张量缩放即便8比特也可能严重退化，敏感组件因模型而异。作者用分阶段消融定位敏感层，逐层GPTQ可恢复到0.1 UTMOS以内，并强调需在目标运行时验证真实内核。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.28974v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 677, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.28974v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 733, \"height\": 349, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.28974v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 889, \"height\": 342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.28974v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 845, \"height\": 317, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.28974v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 866, \"height\": 436, \"label\": \"Table\"}]"
motivation: 端侧TTS部署需要低比特量化，但已有PTQ评测分散，且同一位宽在不同架构上的效果缺乏统一比较。
method: 在统一协议下评测三个核心TTS模型、八个权重/激活消融模型及两个盲测模型，并用分阶段消融定位敏感组件。
result: 4比特逐通道权重使Supertonic的UTMOS降2.8，Kokoro仅降0.07；逐张量缩放8比特也可严重退化，敏感层因模型而异。
conclusion: 逐层GPTQ可将敏感层恢复到0.1 UTMOS内；真实int8/int4内核成本依硬件而异，配置需在目标运行时验证。
---

## 摘要
训练后量化（PTQ）降低了端侧文本到语音（TTS）的成本，但已发表的评估通常只涵盖一个系统或一种方法。我们在同一协议下跨 TTS 架构评估 PTQ，涉及三个核心模型、另外八个模型的权重和激活消融，以及两个盲量化的留出模型。4 比特逐通道权重使 UTMOS（一种预测平均意见得分）在 Supertonic 上降低 2.8，在 Kokoro 上降低 0.07，而逐张量缩放即使在 8 比特下也可能导致严重退化。相同位宽会产生不同结果，因为敏感组件因模型而异，且无法从模型类别可靠预测。分阶段消融流程可以识别该组件，而逐层 GPTQ 能将其恢复到 0.1 UTMOS 以内。真实 int8 和 int4 内核以依赖硬件的代价复现了模拟排序。在 Mac mini 上，4 比特权重内核以 fp32 延迟的 0.60 倍运行 Supertonic，而 int8 更慢，因此每种配置都需要在目标运行时上验证。

## Abstract
Post-training quantization (PTQ) reduces the cost of on-device text-to-speech (TTS), but published evaluations cover one system or method. We evaluate PTQ across TTS architectures under one protocol with three core models, weight and activation ablations of eight more, and two held-out models quantized blind. Four-bit per-channel weights reduce UTMOS, a predicted mean opinion score, by 2.8 on Supertonic and 0.07 on Kokoro, and per-tensor scaling can cause severe degradation even at 8 bits. The same bit width yields different outcomes, because the sensitive component is model-specific and not reliably predicted from the model class. A staged ablation procedure identifies it, and per-layer GPTQ can restore it to within 0.1 UTMOS. Real int8 and int4 kernels reproduce the simulated ordering at hardware-dependent cost. On a Mac mini, a 4-bit weight kernel runs Supertonic at 0.60x the fp32 latency while int8 is slower, so each configuration requires validation on the target runtime.