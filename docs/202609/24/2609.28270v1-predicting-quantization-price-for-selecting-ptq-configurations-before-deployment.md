---
title: Predicting Quantization Price for Selecting PTQ Configurations Before Deployment
title_zh: 预测量化价格以在部署前选择PTQ配置
authors: "Junbin Qiu, Jian Mu, Weitong Zhang, Yao Shu"
date: 2026-09-23
pdf: "https://arxiv.org/pdf/2609.28270v1"
tags: ["query:el"]
score: 8.0
evidence: 后训练量化配置选择，涉及变换与比特
tldr: 权重空间PTQ需在部署前选择格式、粒度、量化器、变换与比特，但现有方法多固定量化几何或分族评分。本文将每层配置视为带部署成本的误差生成器，用全精度到量化前向KL的二阶项定义层输出误差协方差的价格，并由下游曲率定价。通过迹约简得到校准期价格表与预算引导选择器。该框架统一比较不同格式与变换，并把固定几何比特分配变为特例。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.28270v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1436, \"height\": 451, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.28270v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 865, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.28270v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 874, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.28270v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1457, \"height\": 871, \"label\": \"Table\"}]"
motivation: 现有PTQ虽能预测重构误差、Hessian敏感度、变换影响与下游损失，但通常固定量化几何或仅在单一配置族内评分，难以部署前统一选型。
method: 将每层配置视为带部署成本的误差生成器，诱导层输出误差协方差，并由全精度模型下游曲率按迹定价，进而构建价格表与预算引导选择器。
result: 该定价将重构误差和对角评分化为丢弃价格因子的简化代理，使格式、码本、粒度与等价变换可按协方差和成本比较。
conclusion: 固定几何比特分配成为特例而非组织问题，为部署前PTQ配置选择提供统一的价格框架。
---

## 摘要
权重空间训练后量化（PTQ）必须在完整的量化模型揭示其输出分布漂移之前，选择有限格式、粒度、量化器族、变换和比特数。现有的PTQ方法预测了这种退化的重要部分，包括重建误差、Hessian敏感度、变换效应和下游损失，但这些部分通常是在固定量化几何之后或在各自独立的配置族内部进行评分。我们将权重空间PTQ形式化为使用带价格的层输出误差进行部署前配置选择。每个可容许的层配置被视为一个带有部署成本的误差生成器，它诱导层输出误差协方差 $\boldsymbolΣ_l(α_l)$，而全精度模型通过下游曲率对该协方差定价，$\widehatρ_l(α_l)=\frac{1}{2}\operatorname{Tr}\left(\widehat{\mathbf{H}}_l\,\widehat{\boldsymbolΣ}_l(α_l)\right)$。该价格来自全精度到量化模型的前向KL，其一阶项在参考模型处抵消。它将重建评分和对角评分转化为丢弃价格因子的简化代理，而有限格式、码本、粒度和等价变换通过它们诱导的协方差和所付出的成本成为可比较的候选。随后，一个迹缩减产生校准时的价格表和带预算的价格引导选择器，使固定几何比特分配成为一种特例，而不是组织问题。

## Abstract
Weight-space post-training quantization (PTQ) must choose finite formats, granularities, quantizer families, transformations, and bits before the completed quantized model reveals its output-distribution drift. Existing PTQ methods predict important pieces of this degradation, including reconstruction error, Hessian sensitivity, transformation effects, and downstream loss, but these pieces are usually scored after fixing the quantization geometry or inside separate configuration families. We formulate weight-space PTQ as pre-deployment configuration selection using priced layer-output error. Each admissible layer configuration is treated as an error generator with a deployment cost, which induces a layer-output error covariance $\boldsymbolΣ_l(α_l)$, and the full-precision model prices that covariance by downstream curvature, $\widehatρ_l(α_l)=\frac{1}{2}\operatorname{Tr}\left(\widehat{\mathbf{H}}_l\,\widehat{\boldsymbolΣ}_l(α_l)\right)$. The price follows from full-precision-to-quantized forward KL, whose first-order term cancels at the reference model. It turns reconstruction and diagonal scores into reduced proxies that drop price factors, while finite formats, codebooks, granularities, and equivalent transformations become comparable candidates through the covariances they induce and the costs they pay. A trace reduction then yields a calibration-time price table and a budgeted price-guided selector, making fixed-geometry bit allocation a special case rather than the organizing problem.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：权重空间 PTQ 必须在部署前决定每层的有限格式、粒度、量化器族、预量化变换和比特数，但完整量化模型的输出分布漂移只有在所有选择组合完成后才能观察到。
- **现有不足**：已有方法能预测重建误差、Hessian 敏感度、变换效果或下游损失，但通常先固定量化几何，或只在单一配置族内评分，缺少一个可跨格式、码本、粒度、变换和比特的统一部署前比较标准。
- **整体含义**：论文将权重空间 PTQ 重新表述为“部署前配置选择”问题：每个可容许层配置被视为带部署成本的误差生成器，并由全精度参考模型对其诱导的层输出误差协方差定价。固定几何下的比特分配因此成为该框架的一个特例，而非组织中心。

## 2. 方法论

- **核心思想**：目标漂移为全精度与量化模型输出分布之间的前向 KL：
  \[
  J(\alpha)=\mathbb{E}_{x\sim D}D_{\mathrm{KL}}(\pi_0(\cdot|x)\|\pi_\alpha(\cdot|x)).
  \]
  在完整量化模型未知时，用全精度参考模型对候选配置诱导的层输出误差进行局部二阶定价。
- **价格定义**：对层 \(l\)，候选配置 \(\alpha_l\) 诱导误差协方差
  \[
  \Sigma_l(\alpha_l)=\Delta_l(\alpha_l)C_l(\alpha_l)\Delta_l(\alpha_l)^\top,
  \]
  其中 \(C_l\) 是输入协方差，\(\Delta_l\) 是量化权重扰动。参考模型提供输出侧曲率 \(H_l\)，价格定义为
  \[
  \rho_l(\alpha_l)=\frac{1}{2}\operatorname{Tr}(H_l\Sigma_l(\alpha_l)).
  \]
- **理论依据**：Theorem 3.1 表明，从全精度参考到量化模型的前向 KL 展开中，一阶项在参考处抵消，保留的主要局部项正是上述二次价格。重建误差对应将 \(H\) 替换为单位矩阵的简化，对角输入评分进一步将 \(C\) 替换为 \(\operatorname{diag}(C)\)。
- **配置族统一**：
  - 有限格式、比特、粒度、码本改变权重扰动矩，可用矩包络 \(V_l(\alpha_l)\) 上界价格。
  - 等价变换改变量化坐标与输入度量，如 \(X(\alpha)=PX,\ W(\alpha)=WP^{-1}\)，但不改变浮点层输出。
  - 部署成本写为 \(\kappa(\alpha)=\kappa_{\text{bits}}+\kappa_{\text{meta}}+\kappa_{\text{kernel}}\)，进入预算约束。
- **迹缩减与实用评分**：当候选侧输入度量与扰动近似各向同性时，价格可约简
